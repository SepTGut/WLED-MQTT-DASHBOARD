Simple React + Tailwind prototype page.

Open:

`modern-react/index.html`

Notes:
- Uses CDN React/Tailwind/Paho (no build step).
- Includes simple MQTT connect/disconnect.
- Includes quick publish actions (`ON` / `OFF` / `ping`).
- Intended as a lightweight starting point for a fuller migration.
